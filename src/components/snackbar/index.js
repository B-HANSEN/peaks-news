const Snackbar = () => {
	return (
		<div>
			<p>this is a snackbar: ADD</p>
			<p>this is a snackbar: REMOVE</p>
		</div>
	);
};

export default Snackbar;

// TODO: define whether to nest in footer or components?
