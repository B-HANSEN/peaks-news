// Given a loaded website, on any pages.
// When: I click on magnify glass button (1).
// Then: the search box should be expanded with animation(2).

// Given a loaded website, on any pages, and the search box is in focus (2).
// When: typing any query.
// Then: the search site should start performing the search and the loading state should be shown in Search Result.

// Given:
// 1. A loaded website, on any pages.
// 2. The search box is in focus.
// 3. The query has been filled.
// When:
// 1. Remove the query string.
// 2. Click anywhere on the screen apart from the
// search box.
// Then: The search box should shrink back into (1).

const SearchBox = () => {
	return <h1>filter component</h1>;
};

export default SearchBox;
