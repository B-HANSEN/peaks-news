const searchResults = () => {
	return <h1>Search Results Page</h1>;
};

export default searchResults;

// add view bookmarks button & filter

// All cards listed here will be a News. card with an image.
// In case of no image return from the API, use The Peaks image placeholder.
// Cards should be loaded from api by 15 elements and paginate with infinite scroll.
