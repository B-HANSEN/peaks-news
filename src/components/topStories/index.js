const TopStories = () => {
	return <p>Top Stories</p>;
};

export default TopStories;

// Given a loaded website, with all articles listed.
// When: I click on a single news card.
// Then: the site should be navigated to an Article Page.
